/**
 * Prévia visual do guia da Home (primeiro produto / primeira venda / resultado).
 * Não é o onboarding de perfil em `/onboarding`.
 *
 * Liga para iterar no card com a conta atual, mesmo já ativa.
 * Não persiste progresso nem altera dados. Desligue (`false`) depois.
 *
 * Sem `__DEV__`: o PWA de preview é build de produção e esconderia o mock.
 */
export const PREVIEW_HOME_GETTING_STARTED = true;
