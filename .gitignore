# dependencies
node_modules/
.pnp
.pnp.js

# build
dist/
.next/
.expo/
android/
ios/
web-build/

# turbo
.turbo

# env
.env
.env.local
.env.*.local

# ide
.vscode/settings.json
.idea/

# os
.DS_Store
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# test
coverage/
.pnpm-store/
.claude/

# expo
.expo-shared/

# signing — never commit
*.jks
*.keystore
*.pem
*.p12
*.p8
*.mobileprovision
upload_certificate*

# google cloud / service account keys — never commit
lucro-caseiro-*.json
*service-account*.json
*serviceaccount*.json
*-gcp-key*.json

# tooling cache
*.tsbuildinfo
*TEMPknip.txt

# concurrent agent runtime logs / metro cache — never commit
.codex-logs/

# maestro e2e — credenciais (.env já coberto acima) e screenshots (artefatos de teste).
# `**/` pra pegar também em apps/mobile/.maestro/ (não só na raiz).
**/.maestro/.env
**/.maestro/screenshots/

# accidental root expo init
/app.json
