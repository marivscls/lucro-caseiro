import config from "./brand.json";
import type { BrandConfig } from "../types";

// TODO(design): replace the provisional plum palette after design approval.
export const lucroManicureBrand: BrandConfig = config;
